const values = {
    LINKEDIN_AUTH_ID,
    LINKEDIN_AUTH_SECRET,
    GOOGLE_CLOUD_PROJECT_ID,
    GOOGLE_CLOUD_BUCKET,
    DB_USERNAME,
    DB_PASSWORD,
    DB_HOST,
    DB_PORT,
    DB_NAME,
    LINKEDIN_CALLBACK,
    API_URL,
    PORT,
    SWAGGER_HOST,
    SWAGGER_SCHEMES
} = process.env

module.exports = values;